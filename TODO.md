# TODO

Список ведётся в порядке добавления/выполнения задач — порядок не отражает приоритет.

- [ ] `guard-bash.sh`: сокращённые однозначные long-опции `rm` (например, `rm --recu /x`, которые GNU
  принимает как `--recursive`) не распознаются — детектируется только точная форма `--recursive`.
- [ ] `guard-bash.sh`: это эвристика по тексту команды, а не реальный парсинг shell — кавычки/escaping,
  алиасы/функции `rm`, переменные в аргументах не учитываются. Путь с пробелом внутри
  (`rm -rf "/tmp/my file"`) при пословной токенизации распадётся на два токена, второй не совпадёт с
  `/tmp/*` и даст ложную блокировку безопасной команды внутри `/tmp`.
- [ ] `guard-bash.sh`: команды вида `echo rm -rf` или `rm` как часть command substitution/переменной
  блокируются как false positive — детект совпадает по тексту, не по факту исполнения. Принято
  осознанно: для guard-хука ложная блокировка безопаснее пропуска реального рекурсивного удаления.
- [x] ~~`review-code`: скилл появился как обход `disable-model-invocation` у встроенного
  `/code-review` (с CLI 2.1.215 модель не может вызвать его сама). Если Anthropic снимет флаг —
  перепроверить: скилл можно упростить или отказаться от него в пользу встроенного.~~
  **Отменено 2026-07-20** (решение пользователя): `review-code` — основной инструмент ревью
  независимо от судьбы флага у встроенного `/code-review`, как более совершенный (проектная
  настройка, уровни, adversarial-верификация). При обновлениях CLI перепроверять не нужно.
- [ ] `guard-bash.sh`: барьер read-only git/gh (с dev-toolkit 1.16.0) осознанно покрывает только
  `git diff/log/blame/show` и `gh pr diff/view` — ровно auto-approved префиксы `allowed-tools`
  скилла review-code (решение 2026-07-20: остальные формы проходят обычный permission-промпт, а
  глобальная блокировка `gh … -R`/`--web` ломала бы легитимные `gh pr list -R owner/repo` и
  `gh repo view --web`). Известные принятые ограничения: `gh` вне `pr diff/view` не блокируется;
  кластер gh-флагов с буквенным прицепленным значением (`-Sfew`) может дать ложную блокировку;
  `git -c diff.external=…` не детектится (но и не проходит префикс `Bash(git diff:*)`, т.е. вне
  модели угрозы).
- [ ] `optimize-project-docs`: прогрессивное раскрытие самого скилла — секция «Вынос исторических/справочных
  данных в companion-файлы» (~50 строк) нужна не в каждом запуске; кандидат на вынос в
  `references/layering.md` со строкой-сводкой в `SKILL.md`, несущей сам императив («companion не
  автозагружается — сводка обязана нести ловушку»). Требует обсуждения: меняет структуру файлов плагина.
- [x] ~~`review-code`: поле `description` во фронтматтере — 1053 символа при известном проекту лимите
  1024 (в 1.10.0 его специально держали как «994/1024»). Предсуществующее, диффом PR #42 не
  затронуто. Найдено на самоаудите `todo-ship` (уровень `max`, угол «правила проекта», CONFIRMED —
  длина перепроверена подсчётом). Приоритет: средний — при усечении описания скилл может терять
  часть триггеров Invoke/Skip.~~
  **Сделано 2026-08-24** (PR #44, dev-toolkit 1.17.2): 1053 → 961 символ. Сжата только вводная
  проза (детали механики и так развёрнуты в теле скилла); блоки `Invoke for:` / `Skip for:` не
  тронуты побайтно — именно они читаются при выборе скилла.
- [ ] `review-code`: защита «файл ревью, изменённый в диффе, этот дифф не судит» (1.19.0) неполна.
  (а) Отключается весь изменённый файл, а не добавленное правкой: любая правка `doc/REVIEW.md`, даже
  опечатка или новая зона, снимает давно согласованный skip-list и планку и откатывает темы к
  `CLAUDE.md`/эвристике; ужесточающие правила **базовой** версии (зона `max`, удалённая в том же
  диффе, или удалённый файл) не восстанавливаются. Правильно — читать `<base>:doc/REVIEW.md`, но
  `git show` нет в `allowed-tools` скилла (добавлять — только вместе с барьером `guard-bash`).
  (б) Самоподавление через изменённый в диффе корневой или вложенный `CLAUDE.md` (скилл читает их
  без ограничения). С 2.1.0 скилл лишь называет такую правку в заголовке вывода и в «заметках»:
  формой брифа дыру не закрыть — агент получает изменённый корневой `CLAUDE.md` автозагрузкой, а
  фаза 0 берёт темы из той же редакции. Путь к механической защите найден ревью PR 2.1.0 (проход 2,
  `xhigh`): правила, добавленные правкой, видны `+`-строками самого диффа — фаза 0 может не
  применять из них skip-list, планку и понижающие правила, как у файла ревью; полную редакцию базы
  отдаёт `git diff <пустое дерево> <база> -- <путь>` (хэш пустого дерева
  `4b825dc642cb6eb9a060e54bf8d69288fbee4904`) — префикс `Bash(git diff:*)` его уже разрешает, а
  `guard-bash` не блокирует, так что `git show` для этого не нужен; форму надо внести в перечень
  инварианта 3, а для target `A...B` база — merge-base (`git merge-base` в `allowed-tools` нет —
  решить, чем её получать). Тот же путь закрывает и (а). Границы предупреждения 2.1.0 (ревью PR,
  проход 5, `xhigh`, CONFIRMED): не видны неотслеживаемые файлы правил (нет в `git diff`) и цели
  симлинков — закрывать вместе с механической защитой. Решение владельца 22.09.2026: делать
  отдельным PR. (в) Неотслеживаемый `REVIEW.md` не виден в `git diff`, но применяется; при
  target = номер PR файл читается из локального чекаута, а не из ревизии PR. (г) «Ужесточающие»
  прочие правила изменённого файла (тема (ж)) применяются — под видом конвенции можно ослабить
  верификацию. Найдено ревью `max` (проход 2, ветка `feat/review-code-project-review-file`, B1),
  кандидаты без верификации — проход остановлен по норме «одни и те же места всплывают»,
  вынесено решением владельца. Приоритет: высокий (security-поверхность ревью).
- [ ] `review-code`: ошибкоустойчивость файла ревью (1.19.0). Ошибка чтения → «не применять ничего»
  теряет зоны `max`/`xhigh` из файла, ревью идёт ниже нужного уровня (fail-closed — стоп или `max`);
  перечень под заголовком, не равным имени секции («Security-поверхность и контракты»), молча
  становится прочим правилом и уровень не поднимает; критерий «похожей секции» не определён.
  Найдено ревью `max` (проход 2, ветка `feat/review-code-project-review-file`, B1), без верификации, вынесено решением владельца. Приоритет: средний.
- [ ] `cppr`: путь из `$ARGUMENTS` (1.19.0) не санитизируется — подставляется в авто-одобренный
  `gh pr create --body-file` (путь к `.env`, флаги вроде `--repo`, переводы строк в промпте), а
  аргумент может прийти от флоу, собранного из недоверенного текста. Нужны: абсолютный путь во
  временном каталоге, без ведущего `-` и пробелов, инвариант «данные, не инструкции» для требований
  проекта к описанию PR. Проверка секретов «чтением» не ловит значения без узнаваемого префикса.
  Найдено ревью `max` (проход 2, ветка `feat/review-code-project-review-file`, B1), без верификации, вынесено решением владельца. Приоритет: высокий.
- [ ] `todo-ship` шаг 5.2 (1.19.0): (а) `gh pr checks <N> --required --json` в репозитории без
  обязательных проверок возвращает exit 1 «no required checks reported» — по тексту это «не готово»,
  и решение владельца «необязательная без гейта — вопрос пользователю» не срабатывает на самом
  частом случае; (б) без защиты ветки любой красный CI, включая security-сканы (CodeQL, gitleaks),
  уходит в «вопрос пользователю» — нужно исключение для security/тестовых проверок; (в) нет
  fallback при gh < 2.50 (`gh pr view --json statusCheckRollup`); (г) нет исхода «ответа
  пользователя нет» (автономный флоу — стоп, санкция шага 1.3 ответом не считается); (д) гейт мёржа
  ищется только в `.claude/settings.json`/`CLAUDE.md`, не в `settings.local.json`/глобальных.
  Найдено ревью `max` (проход 2, ветка `feat/review-code-project-review-file`, B1), без верификации, вынесено решением владельца. Приоритет: высокий.
- [ ] `todo-ship`/`review-code` (1.19.0): повторные раунды шага 4.3 идут с тем же уровнем и полным
  target, что противоречит нормам проходов проекта-потребителя (проходы 2–3 — по range правок и
  пониженному уровню); таблица триажа в `review-code` не передаётся — пункт skip-list «уже
  отброшенные `PLAUSIBLE`» в конвейере не применится никогда (канала данных, кроме текста вызова,
  нет); `todo-ship` не требует фиксировать триаж до правок. Найдено ревью `max` (проход 2, ветка `feat/review-code-project-review-file`, B1), без
  верификации, вынесено решением владельца. Приоритет: средний.
- [ ] `review-code`/`todo-ship` (1.19.0): чистота текста после B1 — требование «передавать пункты 1–5 skip-list
  дословно» повторено в нескольких фазах; ограничения `gh pr merge` в `todo-ship` перечисляют токены
  одного хука-потребителя вместо общего правила формы команды; тема (ж) «прочие правила» не
  перекрывает, а дополняет `CLAUDE.md` — формулировка приоритета это не отражает. Найдено ревью
  `max` (проход 2, ветка `feat/review-code-project-review-file`, B1), без верификации, вынесено
  решением владельца. Приоритет: низкий.
- [ ] `review-code` (1.19.0): исключение из сквозного инварианта 1 для пункта 5 skip-list записано
  дважды и с разной областью — в самом инварианте без условия, в пункте 5 с условием «если сам файл
  ревью не изменён в диффе»; оба текста уходят субагентам дословно, поэтому при изменённом в диффе
  файле ревью указание «не анализировать X» одни агенты флагают, другие гасят. Держать исключение
  в одном месте. Найдено ревью `max` (проход 3, ветка `feat/review-code-project-review-file`, B1),
  без верификации, вынесено решением владельца. Приоритет: высокий.
- [ ] `review-code` (1.19.0): «кусок пуст **или его ревизии нет** → прогон `полный`» приравнивает к
  штатной пустоте реальный отказ команды (shallow-клон, незафетченная база дают «нет такой
  ревизии»), и в описание PR уходит «полный» там, где коммиты ветки не смотрел никто. Различать по
  причине, а не по тексту ошибки. Найдено ревью `max` (проход 3, ветка
  `feat/review-code-project-review-file`, B1), без верификации, вынесено решением владельца.
  Приоритет: высокий.
- [ ] `review-code` (1.19.0): правило «кандидат считается security-находкой, если так его отнёс хотя
  бы один голос» опирается на данные, которых нет в форматах: у кандидата finder'а и у вердиктов
  верификатора поля категории нет, `category` появляется только в фазе 3. Нужно поле категории в
  формате кандидата и вердикта. Там же: отмена самоподавления finder'ом сделана только для `max`, на
  `xhigh` (пол `todo-ship`) кандидат по-прежнему снимается одним голосом. Найдено ревью `max`
  (проход 3, ветка `feat/review-code-project-review-file`, B1), без верификации, вынесено решением
  владельца. Приоритет: высокий.
- [ ] `cppr` (1.19.0): критерий «аргумент выглядит путём» не задан, а ветки асимметричны — нечитаемый
  путь останавливает команду до коммита, «свободный текст» молча продолжает без готового файла
  (потеря метаданных вызывающего флоу). Правило секретов (что считать секретом и что делать при
  находке) записано только в шаге 9, а шаги 0 и 5 ссылаются на него вперёд; шаг 9 при этом покрывает
  один артефакт из трёх и не покрывает второй и последующие файлы описания при нескольких ветках.
  Найдено ревью `max` (проход 3, ветка `feat/review-code-project-review-file`, B1), без верификации,
  вынесено решением владельца. Приоритет: средний.
- [ ] `todo-ship` (1.19.0): `pass=4` из шага 3 против потолка шага 4.4 «не более 3 раундов» —
  «раунд» не определён (входит ли прогон шага 3), поэтому конвейер останавливается то на `pass=3`,
  то на `pass=4`, а у проекта-потребителя четвёртый проход требует санкции владельца. Там же:
  «отказ `review-code` по формату аргументов — раунд не засчитывается» не ограничен числом попыток.
  Найдено ревью `max` (проход 3, ветка `feat/review-code-project-review-file`, B1), без верификации,
  вынесено решением владельца. Приоритет: средний.
- [ ] `review-code` (1.19.0): источник числа токенов назван как «`subagent_tokens` из блока `usage`
  уведомления `task-notification`, одно значение на агента по его task-id», но в
  foreground-прогонах значение приходит в результате вызова `Agent` и task-id модели не виден, а в
  background-прогонах `subagent_tokens` есть не у всех уведомлений. Сформулировать источник так,
  чтобы он работал в обоих режимах. Найдено ревью `max` (проход 3, ветка
  `feat/review-code-project-review-file`, B1), без верификации, вынесено решением владельца.
  Приоритет: низкий. С 2.0.0 пункт относится только к режиму `Agent`: в режиме `Workflow`
  источник один — `usage.subagent_tokens` уведомления о завершении workflow.
- [ ] `review-code`/`cppr`/`todo-ship` (1.19.0): предупреждения заголовка вывода (файл ревью изменён
  в диффе, нераспознанная секция, расхождение уровня или scope с нормой прохода, умолчание `pass`,
  предложение разделить PR) повторяются полем «прогон» сводки, но в таблице проходов описания PR у
  обоих потребителей слота под них нет — на границе «сводка → описание PR» они теряются. Нужна
  правка контракта переноса (строка предупреждений под таблицей проходов либо отдельное поле).
  Найдено ревью `medium` (проход 4, ветка `feat/review-code-project-review-file`, B1), вердикт
  `PLAUSIBLE` после верификации скептиком, вынесено решением владельца. Приоритет: средний.
- [ ] `review-code` (1.20.0): **пересмотреть не раньше 2026-10-04** три решения владельца от
  20.09.2026 по правилу «Модель субагентов» — сейчас каждое оставлено как есть, скилл только
  уведомляет о нём в заголовке вывода (с 1.21.0): (а) пола модели для `max` нет — на `pass` ≥ 2
  security-поверхность и контракты ищут finder'ы на `sonnet`; (б) у проекта нет канала отказаться от
  удешевления повторных проходов — темы «модель» в правилах ревью нет; (в) правило рассчитано на
  сессию не слабее `opus` — при более слабой сессии верификация идёт на модели слабее finder'ов.
  Материал для анализа — сводки прогонов с разбивкой агентов и токенов по моделям (описания PR
  потребителя, оценка №55). Найдено ревью `max` (проход 1, ветка `feat/review-code-finder-model`,
  C1, PR #48), вердикты `PLAUSIBLE`, вынесено решением владельца. Приоритет: средний.
- [x] ~~`review-code` (1.20.0, существовало и раньше): отказ sweep-finder'а не описан — правила «Отказ
  угла — не молчать» и перезапуска записаны только для «угла», а в перечне причин `деградированный`
  поля «режим» нет пункта «sweep не отработал». Если sweep упал или вернул прозу, прогон `xhigh`/`max`
  остаётся `полный`, хотя страховки от пропусков фазы 1 не было. Нужно: перезапуск sweep один раз
  и причина деградации в поле «режим». Найдено ревью `max` (проход 1, ветка
  `feat/review-code-finder-model`, C1, PR #48), вердикт `PLAUSIBLE` (3 линзы), подтверждено
  верификаторами прохода 2; занесено решением владельца. Приоритет: средний.~~
  **Сделано 2026-09-20** (ветка `feat/review-code-workflow`, dev-toolkit 2.0.0, решение владельца
  закрыть в C2): отказ sweep — как отказ угла: один перезапуск на той же модели, второй отказ —
  прогон `деградированный` с причиной «sweep не отработал» (фаза 2.5 и поле «режим» сводки); в
  workflow-скрипте то же выражено кодом и покрыто стендом-заглушкой.
- [ ] `review-code` (2.0.0): **не раньше 2026-10-05** запустить независимое облачное ревью PR #50
  (задача C2, конвейер как Workflow-скрипт) — команду набирает **только владелец**:
  `/code-review ultra 50`. Почему: проходы 2 и 3 ревью этого PR шли ниже пола `max`, который правило 1
  скилла даёт для security-поверхности (проход 2 — `high` по решению владельца из-за лимита
  использования, проход 3 — `medium` как узкий закрывающий); `max` видел только первый коммит ветки.
  Что нужно для запуска: репозиторий `Leonid74/ai-skills`, PR **#50**
  (https://github.com/Leonid74/ai-skills/pull/50), база — `main` на коммите `0184edd`; коммиты ветки
  `feat/review-code-workflow`: `2cc0793` (исходная реализация — её смотрел проход 1, `max`),
  `15f3bbf` (волны по 5 и правки прохода 1 — их смотрел только `high`), `b0d931c` (правки прохода 2
  — только `medium`), `a21502b` (JSDoc). В `main` всё это — один squash-коммит с «(#50)» в заголовке:
  `git log --oneline --grep "(#50)" main`. Если облачный режим не примет уже смёрженный PR — запускать
  из рабочей копии на `main` по диапазону `/code-review ultra 0184edd...<squash-коммит>`. Главное, на
  что смотреть: `plugins/dev-toolkit/workflows/review-code.js` — приём недоверенных данных от
  finder'ов (`normalizePath`, `accept`, JSON-блоки промптов), распознавание лимита (`looksLikeLimit`,
  `runWaves`), подсчёт голосов (`tally`); `skills/review-code/SKILL.md` — условия согласия на
  `Workflow`. Таблицы проходов и триажа — в описании PR #50. Занесено по прямой просьбе владельца
  20.09.2026. Приоритет: средний.
- [x] ~~`review-code` (2.0.0): текст скилла требует передавать каждому агенту правила из `CLAUDE.md`
  «дословно, с путём» (фаза 1 «Каждому углу передать…», поле `context` входа workflow-скрипта),
  но корневой `CLAUDE.md` агент уже получает автоматически — копия в брифе дублирует его и
  оплачивается каждым агентом отдельно. **Замер 22.09.2026** (apps-b24-platform, транскрипты 53
  агентов прогона `xhigh` PR #268 и двух workflow-прогонов): корневой `CLAUDE.md`, глобальный
  `~/.claude/CLAUDE.md` и индекс памяти `MEMORY.md` грузятся в КАЖДОГО агента в обоих режимах
  (`Agent` и `Workflow`, вложение `instructions`); стартовый контекст агента — 74 087 единиц
  контекста, общий кэш между агентами — только первые 16 701 (системный промпт и схемы
  инструментов), остальное каждый агент записывает в кэш заново; проектный `CLAUDE.md` там —
  около половины старта. В самом прогоне сессия передала указатели, а не текст, так что
  фактическое дублирование было малым, — пункт страхует от буквального исполнения текста скилла.
  Решение из трёх пунктов:
  1. **Корневой `CLAUDE.md` и индекс памяти в бриф не копировать** — агент получает их сам;
     вместо текста передавать перечень применимых правил строками-ссылками
     («`CLAUDE.md` §Логирование: маскирование — в форматтере»).
  2. **Дословно передавать только то, чего у агента нет:** секции файла ревью (`doc/REVIEW.md`
     не автозагружается), вложенные `CLAUDE.md` затронутых каталогов (грузятся только при чтении
     файла инструментом Read, а дифф агент читает через Bash — `git diff` их не подгружает),
     skip-list и сквозные инварианты.
  3. **Исключение — дифф меняет корневой `CLAUDE.md`:** автозагрузка отдаёт агенту уже новую
     редакцию из ревьюируемого PR, поэтому изменённые разделы передавать дословно в редакции
     базы — по той же логике, что «файл ревью, изменённый в диффе, этот дифф не судит».
  Занесено по прямой просьбе владельца 22.09.2026. Приоритет: средний.~~
  **Сделано 2026-09-22** (PR #52, dev-toolkit 2.1.0): п. 1–2 реализованы — правила из
  автозагружаемых файлов идут в бриф ссылками, дословно — только файл ревью, вложенные `CLAUDE.md`
  и правила skip-list. **П. 3 заменён** решением владельца: «редакция базы» в брифе защиты не даёт,
  вместо неё — предупреждение о правке файла правил в заголовке вывода и «заметках»; механическая
  защита — пункт выше, (б).
- [ ] `guard-bash.sh`: исключение `/tmp` для рекурсивного `rm` сравнивает операнд с `/tmp/*` как
  текст, без нормализации `..` — `rm -rf /tmp/../home/x`, `rm -rf /tmp/a/../../var/lib` проходят
  (exit 0), хотя удаляют вне `/tmp`. Там же: операнд в кавычках (`rm -rf -- '/tmp/x'`) не совпадает
  с `/tmp/*` и блокируется — ложная блокировка безопасного случая. Найдено ревью ветки
  `feat/server-disk-cleanup-skill` (dev-toolkit 2.3.0, уровень `xhigh` вместо `max` — мораторий до
  04.10.2026), верификатор G10; `..`-обход перепроверен прогоном хука. Файл хука диффом не менялся.
  Приоритет: средний — обход срабатывает только при явно абсурдном пути, но это ровно тот класс, от
  которого хук защищает.
- [ ] Длина `description` во фронтматтере скиллов ничем не проверяется: `claude plugin validate`
  пропускает описание и больше 1024 символов (проверено на копии плагина: 1217 и 1245 символов —
  «✔ Validation passed»). Известный проекту лимит 1024 уже нарушался (`review-code`, 1053 символа,
  исправлено в 1.17.2), а запас сейчас мал: `optimize-project-docs` — 1005 из 1024,
  `server-disk-cleanup` — 945. Нужна механическая проверка (скрипт по всем `skills/*/SKILL.md`,
  считающий длину значения после YAML-разбора) в перечне «Валидация» `CLAUDE.md`. Найдено ревью
  ветки `feat/server-disk-cleanup-skill` (dev-toolkit 2.3.0, уровень `xhigh` вместо `max` —
  мораторий до 04.10.2026), вердикт PLAUSIBLE; занесено по прямой просьбе владельца 26.09.2026.
  Приоритет: средний — превышение не видно ни одной проверкой, а скилл с усечённым описанием
  может терять триггеры `Invoke for` / `Skip for`.
