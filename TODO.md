# TODO

Список ведётся в порядке добавления/выполнения задач — порядок не отражает приоритет.

- [ ] `guard-bash.sh`: regex проверяет только первый токен флагов сразу после `rm` — `rm -v -r file` (recursive не первым флагом) не поймается. Ограничение существовало и до правки compound-команд/`/tmp`, не специфично для неё.
- [ ] `guard-bash.sh`: длинная форма `--recursive` не детектируется. Расширять detection на неё нельзя без отдельной правки sed-стриппера в leftover-логике — иначе сломается `/tmp`-исключение для этой формы.
