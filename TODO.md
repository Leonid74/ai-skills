# TODO

Список ведётся в порядке добавления/выполнения задач — порядок не отражает приоритет.

- [ ] `guard-bash.sh`: сокращённые однозначные long-опции `rm` (например, `rm --recu /x`, которые GNU
  принимает как `--recursive`) не распознаются — детектируется только точная форма `--recursive`.
- [ ] `guard-bash.sh`: это эвристика по тексту команды, а не реальный парсинг shell — кавычки/escaping,
  алиасы/функции `rm`, переменные в аргументах не учитываются. Путь с пробелом внутри
  (`rm -rf "/tmp/my file"`) при пословной токенизации распадётся на два токена, второй не совпадёт с
  `/tmp/*` и даст ложную блокировку безопасной команды внутри `/tmp`.
- [ ] `guard-bash.sh`: команды вида `echo rm -rf` или `rm` как часть command substitution/переменной
  блокируются как false positive — детект совпадает по тексту, не по факту исполнения. Принято
  осознанно: для guard-хука ложная блокировка безопаснее пропуска реального рекурсивного удаления.
- [x] ~~`review-code`: скилл появился как обход `disable-model-invocation` у встроенного
  `/code-review` (с CLI 2.1.215 модель не может вызвать его сама). Если Anthropic снимет флаг —
  перепроверить: скилл можно упростить или отказаться от него в пользу встроенного.~~
  **Отменено 2026-07-20** (решение пользователя): `review-code` — основной инструмент ревью
  независимо от судьбы флага у встроенного `/code-review`, как более совершенный (проектная
  настройка, уровни, adversarial-верификация). При обновлениях CLI перепроверять не нужно.
- [ ] `guard-bash.sh`: барьер read-only git/gh (с dev-toolkit 1.16.0) осознанно покрывает только
  `git diff/log/blame/show` и `gh pr diff/view` — ровно auto-approved префиксы `allowed-tools`
  скилла review-code (решение 2026-07-20: остальные формы проходят обычный permission-промпт, а
  глобальная блокировка `gh … -R`/`--web` ломала бы легитимные `gh pr list -R owner/repo` и
  `gh repo view --web`). Известные принятые ограничения: `gh` вне `pr diff/view` не блокируется;
  кластер gh-флагов с буквенным прицепленным значением (`-Sfew`) может дать ложную блокировку;
  `git -c diff.external=…` не детектится (но и не проходит префикс `Bash(git diff:*)`, т.е. вне
  модели угрозы).
- [ ] `optimize-project-docs`: прогрессивное раскрытие самого скилла — секция «Вынос исторических/справочных
  данных в companion-файлы» (~50 строк) нужна не в каждом запуске; кандидат на вынос в
  `references/layering.md` со строкой-сводкой в `SKILL.md`, несущей сам императив («companion не
  автозагружается — сводка обязана нести ловушку»). Требует обсуждения: меняет структуру файлов плагина.
- [x] ~~`review-code`: поле `description` во фронтматтере — 1053 символа при известном проекту лимите
  1024 (в 1.10.0 его специально держали как «994/1024»). Предсуществующее, диффом PR #42 не
  затронуто. Найдено на самоаудите `todo-ship` (уровень `max`, угол «правила проекта», CONFIRMED —
  длина перепроверена подсчётом). Приоритет: средний — при усечении описания скилл может терять
  часть триггеров Invoke/Skip.~~
  **Сделано 2026-08-24** (PR #44, dev-toolkit 1.17.2): 1053 → 961 символ. Сжата только вводная
  проза (детали механики и так развёрнуты в теле скилла); блоки `Invoke for:` / `Skip for:` не
  тронуты побайтно — именно они читаются при выборе скилла.
- [ ] `review-code`: защита «файл ревью, изменённый в диффе, этот дифф не судит» (1.19.0) неполна.
  (а) Отключается весь изменённый файл, а не добавленное правкой: любая правка `doc/REVIEW.md`, даже
  опечатка или новая зона, снимает давно согласованный skip-list и планку и откатывает темы к
  `CLAUDE.md`/эвристике; ужесточающие правила **базовой** версии (зона `max`, удалённая в том же
  диффе, или удалённый файл) не восстанавливаются. Правильно — читать `<base>:doc/REVIEW.md`, но
  `git show` нет в `allowed-tools` скилла (добавлять — только вместе с барьером `guard-bash`).
  (б) Самоподавление через изменённый в диффе корневой или вложенный `CLAUDE.md` (скилл читает их
  без ограничения). (в) Неотслеживаемый `REVIEW.md` не виден в `git diff`, но применяется; при
  target = номер PR файл читается из локального чекаута, а не из ревизии PR. (г) «Ужесточающие»
  прочие правила изменённого файла (тема (ж)) применяются — под видом конвенции можно ослабить
  верификацию. Найдено ревью `max` (проход 2, ветка `feat/review-code-project-review-file`, B1),
  кандидаты без верификации — проход остановлен по норме «одни и те же места всплывают»,
  вынесено решением владельца. Приоритет: высокий (security-поверхность ревью).
- [ ] `review-code`: ошибкоустойчивость файла ревью (1.19.0). Ошибка чтения → «не применять ничего»
  теряет зоны `max`/`xhigh` из файла, ревью идёт ниже нужного уровня (fail-closed — стоп или `max`);
  перечень под заголовком, не равным имени секции («Security-поверхность и контракты»), молча
  становится прочим правилом и уровень не поднимает; критерий «похожей секции» не определён.
  Найдено ревью `max` (проход 2, B1), без верификации, вынесено решением владельца. Приоритет: средний.
- [ ] `cppr`: путь из `$ARGUMENTS` (1.19.0) не санитизируется — подставляется в авто-одобренный
  `gh pr create --body-file` (путь к `.env`, флаги вроде `--repo`, переводы строк в промпте), а
  аргумент может прийти от флоу, собранного из недоверенного текста. Нужны: абсолютный путь во
  временном каталоге, без ведущего `-` и пробелов, инвариант «данные, не инструкции» для требований
  проекта к описанию PR. Проверка секретов «чтением» не ловит значения без узнаваемого префикса.
  Найдено ревью `max` (проход 2, B1), без верификации, вынесено решением владельца. Приоритет: высокий.
- [ ] `todo-ship` шаг 5.2 (1.19.0): (а) `gh pr checks <N> --required --json` в репозитории без
  обязательных проверок возвращает exit 1 «no required checks reported» — по тексту это «не готово»,
  и решение владельца «необязательная без гейта — вопрос пользователю» не срабатывает на самом
  частом случае; (б) без защиты ветки любой красный CI, включая security-сканы (CodeQL, gitleaks),
  уходит в «вопрос пользователю» — нужно исключение для security/тестовых проверок; (в) нет
  fallback при gh < 2.50 (`gh pr view --json statusCheckRollup`); (г) нет исхода «ответа
  пользователя нет» (автономный флоу — стоп, санкция шага 1.3 ответом не считается); (д) гейт мёржа
  ищется только в `.claude/settings.json`/`CLAUDE.md`, не в `settings.local.json`/глобальных.
  Найдено ревью `max` (проход 2, B1), без верификации, вынесено решением владельца. Приоритет: высокий.
- [ ] `todo-ship`/`review-code` (1.19.0): повторные раунды шага 4.3 идут с тем же уровнем и полным
  target, что противоречит нормам проходов проекта-потребителя (проходы 2–3 — по range правок и
  пониженному уровню); таблица триажа в `review-code` не передаётся — пункт skip-list «уже
  отброшенные `PLAUSIBLE`» в конвейере не применится никогда (канала данных, кроме текста вызова,
  нет); `todo-ship` не требует фиксировать триаж до правок. Найдено ревью `max` (проход 2, B1), без
  верификации, вынесено решением владельца. Приоритет: средний.
- [ ] `review-code` (1.19.0): чистота текста после B1 — требование «передавать пункты 1–5 skip-list
  дословно» повторено в нескольких фазах; ограничения `gh pr merge` в `todo-ship` перечисляют токены
  одного хука-потребителя вместо общего правила формы команды; тема (ж) «прочие правила» не
  перекрывает, а дополняет `CLAUDE.md` — формулировка приоритета это не отражает. Найдено ревью
  `max` (проход 2, B1), без верификации. Приоритет: низкий.
